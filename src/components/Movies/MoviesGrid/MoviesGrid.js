// import { MoviesItem } from './MoviesItem';

// export const MoviesGrid = ({removeMovie, moviesToShow }) => {
//   return (
//     <div className='movies__grid'>
//       {moviesToShow.map((movie, idx) => (
//         <MoviesItem movie={movie} key={Date.now()+idx} removeMovie={removeMovie} liked={movie.liked}/>
//       ))}
//     </div>
//   );
// };
